import React from 'react';
import Button from 'react-bootstrap/Button';
import './Button.css';

function button(props) {
    return (
        <button id="button" className="button outline">CV</button>
    );
};

export default button;
